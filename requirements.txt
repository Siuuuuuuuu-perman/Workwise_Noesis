# Minimal FastAPI dependencies for Vercel
fastapi==0.114.0
uvicorn==0.30.6
pydantic==2.9.2
python-multipart==0.0.9

# HTTP client
requests==2.32.3

# PDF processing (lightweight)
pypdf==4.3.1

# Environment management
python-dotenv==1.0.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Note: Removed heavy dependencies (pandas, numpy, streamlit, PyPDF2, sqlalchemy, etc.)
# The app will use lightweight alternatives and graceful fallbacks